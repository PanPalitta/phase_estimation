#ifndef RNG_H
#define RNG_H

using namespace std;

class Rng {
    public:
        ~Rng() {};
        double next_grand(const double mean, const double dev) {
            return 0.0;
            };
        double next_urand() {
            return 0.0;
            };
    };

#endif // RNG_H
